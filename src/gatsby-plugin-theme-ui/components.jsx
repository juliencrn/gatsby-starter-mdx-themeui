import React from 'react'

// Customize MDX only components from HTML tag
export default {
  // h1: props => (
  //   <h1 {...props}>
  //     <a href={`#${props.id}`}>{props.children}</a>
  //   </h1>
  // )
}
